#include "main.h"

/**
 * mul- multiplies 2 integers
 * @a: first integer
 * @b: second integer
 * Return: product of the 2 integers
 */

int mul(int a, int b);

int mul(int a, int b)
{
	return (a * b);
}

Pointers are special variables which contain address of another variables.
It contains an address of variable whose data type is either (float,integer,char etc).
Declaration
          Datatype *pointer name
          int *p;
initialization -> p = &a;

          
